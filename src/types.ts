/**
 * Options for customizing the easing preset.
 * 用于自定义缓动预设的选项。
 */
export interface EasePresetOptions {
  /**
   * Prefix to be added to easing function names.
   * 要添加到缓动函数名称的前缀。
   *
   * @default 'ease-'
   */
  prefix?: string;
}
